% Execution Control Seams — vivid with FA icons, dotted orange feedback
\begin{tikzpicture}[
  box/.style={draw, rounded corners=2pt, align=center, text width=28mm,
              minimum height=10mm, font=\footnotesize, inner sep=2pt},
  ctrl/.style={draw, rounded corners=2pt, align=center, text width=26mm,
               minimum height=8mm, font=\footnotesize, inner sep=2pt, fill=gray!4},
  side/.style={draw, rounded corners=3pt, fill=blue!6, align=center, text width=22mm,
               minimum height=12mm, font=\footnotesize, inner sep=2pt},
  note/.style={font=\scriptsize, text=gray!70!black, align=center},
  arr/.style={-{Stealth[length=2mm]}, thick, shorten <=2pt, shorten >=2pt},
  feedback/.style={-{Stealth[length=2mm]}, densely dotted, orange!70!black}
]

% Telemetry sidebar
\node[side] (tele) at (0, 0.3) {\faBarChart\ \textbf{Telemetry}\\topology, skew};

% Three seams
\node[box, fill=green!5]  (repr)   at (3.6, 1.2) {\faCompress\ \textbf{Representation}\\compressed tuples};
\node[box, fill=yellow!6] (path)   at (6.8, 1.2) {\faServer\ \textbf{Execution Path}\\device mapping};
\node[box, fill=purple!5] (budget) at (10.0,1.2) {\faShield\ \textbf{Boundary}\\quality/latency cap};

% Controls below each seam
\node[ctrl] (c1) at (3.6, -0.3) {format switch\\sampling rate};
\node[ctrl] (c2) at (6.8, -0.3) {CPU/GPU choice\\transfer timing};
\node[ctrl] (c3) at (10.0,-0.3) {compensation\\fallback};

% Metrics
\node[note] at (3.6, -1.3) {compression ratio};
\node[note] at (6.8, -1.3) {movement cost};
\node[note] at (10.0,-1.3) {boundary failures};

% Telemetry -> seams
\draw[arr] (tele.east) -- ++(0.4,0) |- (repr.west);

% Seam flow
\draw[arr] (repr.east) -- (path.west);
\draw[arr] (path.east) -- (budget.west);

% Seam -> control
\draw[arr] (repr.south) -- (c1.north);
\draw[arr] (path.south) -- (c2.north);
\draw[arr] (budget.south) -- (c3.north);

% Feedback (dotted orange, routed right then up)
\draw[feedback] (c3.east) -- ++(0.4,0) |- ([yshift=2mm]tele.east);
\node[font=\scriptsize, text=orange!70!black] at (10.8, 0.6) {\faRetweet\ retuning};

\end{tikzpicture}
